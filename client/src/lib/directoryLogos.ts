/** Re-export shared directory logo resolution for client imports. */
export {
  DIRECTORY_LOGO_VERSION,
  normalizeDirectoryName,
  resolveDirectoryLogo,
  directoryFallbackLogo,
  hasDirectoryLogo,
} from "@shared/directoryLogos";
